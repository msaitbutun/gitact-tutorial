name: Test Pipeline

on:
    push:
        branches:
        - master
    workflow_dispatch:
jobs:
    test: 
        runs-on: ubuntu-latest

        steps:
         - name: Checkout İslemi
           uses: actions/check-out@v4 
         - name: Enviromentleri listele
           run: printenv
    
